@model IEnumerable<Client.Models.School>

@{
    ViewData["Title"] = "School List";
}

<div class="my-3 d-md-flex justify-content-evenly">
    <div class="flex-grow-1 display-6 text-sm-center text-primary opacity-75">School List</div>
    <div class="text-end">
        <a asp-controller="School" asp-action="Create" class="btn btn-primary">New School</a>
    </div>
</div>

@if (Model.Count() < 1) {
    <h2 class="display-6 mt-3 text-center text-danger">Not schools yet</h2>
} else {
    <div class="table-responsive">
        <table class="table table-dark table-striped table-hover">
            <thead>
                <tr>
                    <th>@Html.DisplayNameFor(m => m.Code)</th>
                    <th>@Html.DisplayNameFor(m => m.Name)</th>
                    <th>@Html.DisplayNameFor(m => m.Faculty)</th>
                    <th>Mantainers</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model) {
                    <tr>
                        <td>@item.Code</td>
                        <td>@item.Name</td>
                        <td>@item.Faculty.Name</td>
                        <td>
                            <a asp-controller="School" asp-action="Edit" asp-route-Id="@item.Id" class="btn btn-primary">Edit</a>
                            <a asp-controller="School" asp-action="Details" asp-route-Id="@item.Id" class="btn btn-secondary">Details</a>
                            <a asp-controller="School" asp-action="Delete" asp-route-Id="@item.Id" class="btn btn-danger">Delete</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
}


@section scripts {
    @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); }
}